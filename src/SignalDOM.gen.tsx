/* TypeScript file generated from SignalDOM.re by genType. */
/* eslint-disable import/first */


// tslint:disable-next-line:no-var-requires
const SignalDOMBS = require('./SignalDOM.bs');

import {t as Signal_t} from './Signal.gen';

export const keyPressed: (_1:number) => Signal_t<boolean> = SignalDOMBS.keyPressed;
